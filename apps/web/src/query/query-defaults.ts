/**
 * Общие параметры TanStack Query: меньше лишних сетевых раундов при навигации и возврате на вкладку.
 * Списки рейсов/партий меняются не каждую секунду — разумный staleTime снижает нагрузку на API и ускоряет UI.
 */
export const QUERY_STALE_LISTS_MS = 120_000;

/**
 * Отчёт по рейсу дорогой (БД + агрегации), но продажа могла прийти с другого устройства —
 * короче staleTime + `refetchOnWindowFocus` / `visibilitychange` дают актуальные кг без «миллиона» перелогинов.
 */
export const QUERY_STALE_SHIPMENT_REPORT_MS = 45_000;

/** Подбор рейса в форме (ограниченная выборка) — чаще обновлять, чем полный список. */
/** Держать выгруженные данные в памяти после ухода со страницы; вместе с персистом в localStorage — комфортный офлайн «назад». */
export const QUERY_GC_MS = 6 * 60 * 60 * 1000;
